export enum ContractType {
  PERMANENT = "Permanent",
  CONTRACT = "Contract",
}

export enum WorkType {
  FULL_TIME = "Full-Time",
  PART_TIME = "Part-Time",
}

export enum FormType {
  ADD,
  EDIT,
}
